`silero_vad_v6.onnx` is Silero VAD v6 (MIT license, https://github.com/snakers4/silero-vad),
the copy bundled with faster-whisper. The browser worker runs it with the same speech rule
as `dua_recognition.asr.speech_in_tail`.
